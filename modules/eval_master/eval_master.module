<?php
// $Id$
/**
 * Implementation of hook_menu
 */
function eval_master_menu () { 
  $items = array();

  $items['formation/master_eval'] = array(
    'title' => "Eval Master", 
    'description' => 'Course evaluation module, with forms',
    'page callback' => 'eval_master_page_content',
    'access arguments' => array('access content'),
    'type' => MENU_NORMAL_ITEM,
  );
  return $items;
}

/**
 * Page callback
 */
function eval_master_page_content () {
  return 'Constater Return page callback';  /*  Ce que la page va afficher  */
}
